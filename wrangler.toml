name = "telegrambottime"
compatibility_date = "2024-04-05"
main = "src/index.ts"

# Set environment variables here (non-sensitive)
# [vars]

# Secrets (Add via `npx wrangler secret put <NAME>`)
# SUPABASE_URL
# SUPABASE_ANON_KEY
# TELEGRAM_BOT_TOKEN
# AI_API_KEY
# TELEGRAM_CHAT_ID

[triggers]
# Every minute — handles prayer alerts, task reminders, and check-ins
crons = ["* * * * *"]
